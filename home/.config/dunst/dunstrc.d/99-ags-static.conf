# Keep placement stable when older Dunst drop-ins are present.
[global]
    layer = overlay
    origin = top-right
    offset = -2x17
    follow = mouse
    frame_width = 2
    corner_radius = 14
    corners = left
