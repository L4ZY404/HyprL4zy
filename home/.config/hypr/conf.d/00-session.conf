# Portable Hyprland session. Machine-specific overrides belong in local.conf.
monitor = , preferred, auto, auto

# Keep session services independent from any desktop shell implementation.
exec-once = ~/.config/hypr/scripts/session-start.sh

# Load the current Pywal-compatible palette.
source = ~/.config/hypr/colors.conf

env = XCURSOR_SIZE,32
