dwindle {
    preserve_split = true
}

# -----------------------------------------------------
# Layer and window rules
# -----------------------------------------------------
# Keep notification motion available while the new shell is rebuilt.
layerrule = animation slide, match:namespace = ^(notifications|dunst)$

# Add shell-specific layer rules here only when the new Quickshell surfaces exist.
